""" En este caso utilizamos (Instalada en la cnsola Git) el numpy """
import numpy as np

numeros = [1, 2, 3, 4, 5]
print(np.mean(numeros))    # → 3.0  (promedio)
print(np.sum(numeros))     # → 15   (suma)
print(np.max(numeros))     # → 5    (máximo)
print(np.min(numeros))     # → 1    (mínimo)